==================================================
PHASE 12 — TASK BOARD
=====================
 
PROMPT:
 
Implement the Task Board as a HUD overlay panel/drawer that opens on top of the 3D world (per the UI/UX PARADIGM section) — not a separate route that replaces the 3D view.
 
Columns:
 
Backlog
To Do
In Progress
Review
Done
 
Features:
 
Create task
Edit task
Delete task
Assign user
Priority
Due date
Tags
Comments
Attachments
Search
Filter
Drag-and-drop
 
Use optimistic UI where appropriate.
 
Persist all changes to the backend.
 
Record meaningful task activity history.
 
Ensure the board is fully usable via keyboard (drag-and-drop must have a non-drag fallback, e.g. a status dropdown, to satisfy WCAG 2.1 AA).